def retirar_mascara (a):
    cpfseparado = a.split("." & "-")
    cpfsemmasc ="".join(cpfseparado)
    return (cpfsemmasc)


def inserir_mascara (b):
    a.

cpf = (input("Digite seu CPF:"))

codigo = input("Digite 1 para CPF com máscara e 2 para CPF sem máscara:")

if codigo == 1:
    print(retirar_mascara(cpf))

elif codigo == 2:
    print
